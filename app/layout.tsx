import type { Metadata } from "next";
import { Inter, Playfair_Display } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  weight: ["300", "400", "500", "600"],
  variable: "--font-inter",
  display: "swap",
});

const playfair = Playfair_Display({
  subsets: ["latin"],
  weight: ["400", "500", "600"],
  style: ["normal", "italic"],
  variable: "--font-playfair",
  display: "swap",
});

export const metadata: Metadata = {
  title: "The Burger — A Craft Experience",
  description: "Eight meticulously crafted layers. One unforgettable experience. Discover the precision, passion, and perfection in every bite.",
  keywords: ["premium burger", "craft burger", "gourmet", "luxury food experience"],
  openGraph: {
    title: "The Burger — A Craft Experience",
    description: "Eight layers. One vision. A burger that transcends the ordinary.",
    type: "website",
  },
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en" className={`${inter.variable} ${playfair.variable}`}>
      <body className={`${inter.className} antialiased`}>
        {children}
      </body>
    </html>
  );
}
